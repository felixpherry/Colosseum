# V1.0 MVP — Implementation Tracker

## Phase 1: Foundation

- [x] Monorepo scaffold (Turborepo + pnpm workspaces)
- [x] Package structure (db, trpc, lib, types, ui, config)
- [x] ESLint + Prettier + Husky + lint-staged
- [x] Database schema — users, accounts
- [x] Database schema — tournaments (CHECK constraints)
- [x] Database schema — submissions (UNIQUE per user per tournament)
- [x] Database schema — matchups (6 CHECK constraints)
- [x] Database schema — votes (immutable, SET NULL on user delete)
- [x] Database schema — user_ratings (ELO per category)
- [x] Database schema — tournament_stats (trending cache)
- [x] Drizzle config + push to Supabase
- [x] Auth.js v5 (Google + GitHub OAuth, JWT sessions)
- [x] Auth middleware for protected routes

## Phase 2: Core Domain

- [x] tRPC initialization (context, public/protected procedures)
- [x] Server-side caller (createCallerFactory)
- [x] Client-side provider (TRPCProvider + QueryClient)
- [x] HTTP handler (catch-all API route)
- [x] Tournament CRUD (create, list, getBySlug, listByCategory, getMyTournaments)
- [x] Tournament state transitions (openSubmissions, cancel)
- [x] Submission procedures (submit with duplicate protection, listByTournament)
- [x] Slug generator + totalRounds utility
- [x] Bracket generation — generateSeedOrder
- [x] Bracket generation — calculateBracketSize (auto-shrink)
- [x] Bracket generation — generateBracketData (round 1 + empty later rounds)
- [x] Bracket pipeline — insertBracket (batch INSERT + status transition)
- [x] Bracket pipeline — advanceByes (UPDATE existing round 2 matchups)
- [x] Bracket pipeline — activateReadyMatchups (single UPDATE with WHERE)
- [x] startTournament mutation (wires full pipeline)
- [x] Shared Zod schemas in packages/types

## Phase 3: Core Loop

- [x] Vote cast procedure (transactional, FOR UPDATE, atomic increment)
- [x] Matchup resolution — resolveMatchup (winner + advance + activate)
- [x] Matchup resolution — determineWinner (votes or seed tiebreak)
- [x] ELO rating updates inside resolveMatchup
- [x] pg-boss setup (singleton, getBoss)
- [x] Cron: close-expired-matchups (every 60s)
- [x] Worker: resolve-matchup (singletonKey dedup)
- [x] Rate limiting (Upstash Redis, IP + user limits)
- [x] IP hashing (HMAC-SHA256)

## Phase 4: Discovery

- [x] Full-text search (GIN index, weighted tsvector, websearch_to_tsquery)
- [x] Trending refresh (HN gravity formula, pg-boss every 5 min)
- [x] Trending procedure (reads from tournament_stats)

## Phase 5: Frontend — Core Pages

- [x] Design system tokens (Tailwind v4, CSS custom properties)
- [x] ShadCN setup + token overrides
- [x] Theme provider (next-themes, class-based dark mode)
- [x] GlobalTickerProvider (1Hz shared setInterval)
- [x] useCountdown hook (h/m/s from global tick)
- [x] CountdownTimer component (active/passive/resolved variants)
- [x] VoteCard component (action stack layout, isVoteable gate)
- [x] Vote Now feed (split active/recent, collapsible)
- [x] Create tournament page (TanStack Form + Zod validation)
- [x] Tournament detail page — draft state (open submissions button)
- [x] Tournament detail page — accepting_submissions (submit form + entry list)
- [x] Tournament detail page — in_progress (matchup list + voting)
- [x] Tournament detail page — completed (champion display)
- [x] Tournament detail page — cancelled state
- [x] listMatchups procedure with entry details
- [x] MatchupList component (active/completed/pending sections)

## Phase 5: Frontend — Remaining Pages

- [ ] Login page (OAuth buttons, redirect logic)
- [ ] Profile page (user tournaments, ELO ratings per category)
- [ ] Discovery/browse page (search + trending feed)
- [ ] Bracket visualization (tree view, mobile + desktop)
- [ ] VoteModal (sheet on mobile, dialog on desktop)

## Phase 6: Production Readiness

- [ ] Environment validation (@t3-oss/env-nextjs)
- [ ] pg-boss startup wiring (where does startCronJobs run?)
- [ ] Error handling (toast notifications, error boundaries)
- [ ] Loading states (skeletons, suspense boundaries)
- [ ] Optimistic updates for voting
- [ ] Polling for active matchups (5s interval)
- [ ] Seed script (sample data for development)
- [ ] Integration tests — vote flow (create → submit → bracket → vote → resolve)
- [ ] Integration tests — bracket pipeline (various sizes, BYE scenarios)
- [ ] Integration tests — ELO calculations
- [ ] E2E tests (Playwright — full user journey)
- [ ] Deploy pipeline (Vercel + Supabase)
- [ ] CLAUDE.md symlinked to AGENTS.md

## Blocked / Deferred

- Orphan detector (resolutionEnqueuedAt + resolutionAttempts columns) — deferred to V2
- WebSocket live updates — polling is sufficient for V1
- Redis session store — JWT sessions used for V1
- Bracket shareable images — V2 feature
- Admin role for cancelling in_progress tournaments — V2
